const express = require("express");
const router = express.Router();

// ===== Importer les routes ici =====
// const exampleRoutes = require("./example.route");

// ===== Monter les routes ici =====
// router.use("/examples", exampleRoutes);

module.exports = router;
