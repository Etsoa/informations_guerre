const sequelize = require("../config/database");

// ===== Importer les modèles ici =====
// const Example = require("./Example");

// ===== Définir les associations ici =====
// Example.hasMany(OtherModel);
// OtherModel.belongsTo(Example);

module.exports = {
  sequelize,
  // Example,
};
